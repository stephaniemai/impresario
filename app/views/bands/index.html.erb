<div class="container">
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <div class=box>
        <h1>Où cherchez-vous un musicien ?</h1>
          <%= form_tag bands_path, method: :get do %>
          <%= text_field_tag :query,
          params[:query],
          class: "form-control",
          placeholder: "Find a band"
          %>
        <% end %>
    </div>
    <% if params[:query].present? %>
      <h2>Musiciens autour de <%= params[:query]%> </h2>
      <% else %>
      <% end %>
    </div>
  </div>
</div>
<div class="container">
    <div class="row">
      <% @bands.each do |band| %>
      <div class="col-xs-12 col-sm-4 ">
        <%= link_to band_path(band) do%>
        <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url( <%= cl_image_path band.image %>);">
          <div class="card-category">
            <%= band.genre.name %>
          </div>
          <div class="card-description">
            <h2> <%= band.name %></h2>
          </div>
          <!-- <a class="card-link" href="band_path(band)" ></a> -->

        </div>
         <% end %>
      </div>
    <% end %>
  </div>
</div>
<div class= "ajout_de_groupe">
<p><%= link_to "Ajouter mon groupe", new_band_path, class: "btn btn-primary" %></p>
</div>
